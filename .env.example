# ATLAS Demo — Idea → SaaS → Customer
# Copy to .env and fill in your values

# Chrome profile (for reused logins)
ATLAS_BROWSER_PROFILE=/path/to/Chrome

# LLM navigation
OPENAI_API_KEY=sk-...

# Stripe (test mode). Run create-stripe-product.sh to generate STRIPE_PRICE_ID
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_PRICE_ID=price_...

# Vercel API (get token from vercel.com/account/tokens)
VERCEL_TOKEN=...

# Resend (resend.com - get API key from resend.com/api-keys)
RESEND_API_KEY=re_....
FROM_EMAIL=onboarding@resend.dev

# Customer to notify
CUSTOMER_EMAIL=demo@example.com
